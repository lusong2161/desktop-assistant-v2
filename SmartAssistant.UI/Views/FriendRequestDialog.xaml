<Window x:Class="SmartAssistant.UI.Views.FriendRequestDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Friend Request" Height="200" Width="400"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock>
            <Run Text="Friend request from "/>
            <Run Text="{Binding SenderName}" FontWeight="Bold"/>
        </TextBlock>

        <TextBlock Grid.Row="1" Text="{Binding Message}" 
                   TextWrapping="Wrap" Margin="0,10"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" 
                    HorizontalAlignment="Right">
            <Button Content="Reject" Width="80" Margin="0,0,10,0"
                    Command="{Binding RejectCommand}"/>
            <Button Content="Accept" Width="80"
                    Command="{Binding AcceptCommand}"/>
        </StackPanel>
    </Grid>
</Window>
